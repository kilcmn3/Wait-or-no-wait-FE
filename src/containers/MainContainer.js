import React from 'react';
import { WaitListContainer } from '../exportFiles';

const MainContainer = () => {
  return (
    <div>
      <WaitListContainer />
    </div>
  );
};

export default MainContainer;
